<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2020, Salesforce.com, Inc.
  ~ All rights reserved.
  ~ SPDX-License-Identifier: BSD-3-Clause
  ~ For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>cetrax-parent</artifactId>
        <groupId>io.cetrax</groupId>
        <version>0.5.25-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>cetrax-server</artifactId>
    <name>cetrax-server</name>
    <packaging>jar</packaging>

    <description>Home of the Cetrax server capable of starting in HTTP, gRPC, or both</description>

    <properties>
        <typesafe-config.version>1.4.2</typesafe-config.version>
        <mysql.version>8.0.28</mysql.version>
    </properties>

    <dependencies>
        <!--CANTOR GRPC PROTOS-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-grpc-protos</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--CANTOR GRPC-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-grpc-service</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--CANTOR H2-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-h2</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--CANTOR MYSQL-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-mysql</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--MYSQL-->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>${mysql.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.google.protobuf</groupId>
                    <artifactId>protobuf-java</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!--CANTOR S3-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-s3</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--CANTOR MISC-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-misc</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--TYPESAFE CONFIGURATIONS-->
        <dependency>
            <groupId>com.typesafe</groupId>
            <artifactId>config</artifactId>
            <version>${typesafe-config.version}</version>
        </dependency>
        <!--LOGBACK-->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
        <!--SLF4J BRIDGE-->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jul-to-slf4j</artifactId>
        </dependency>

        <!--TEST SCOPE-->
        <!--TESTNG -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>test</scope>
        </dependency>
        <!--CANTOR COMMON TEST JAR-->
        <dependency>
            <groupId>io.cetrax</groupId>
            <artifactId>cetrax-common</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--ASSEMBLY-->
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.5.3</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>io.cetrax.server.Application</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <finalName>cetrax-server</finalName>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
